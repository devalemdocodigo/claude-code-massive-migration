{
  "name": "quote-service-demo",
  "version": "1.0.0",
  "description": "Projeto didático: serviço de cotação de pedidos, usado para praticar remoção segura de código e testes obsoletos com Claude Code.",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
